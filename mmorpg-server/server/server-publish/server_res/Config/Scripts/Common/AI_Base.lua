--出生
function OnBirth(npc)
end

--空闲状态心跳逻辑
function OnIdleHeartbeat(npc, now)
end

--战斗状态心跳逻辑
function OnFightHeartbeat(npc, now)
end

--进入战斗状态
function OnEnterFightState(npc, now)
end

--离开战斗状态
function OnLeaveFightState(npc, now)
end

--被别人攻击
function OnAttacked(npc, attacker)
end

--攻击别人
function OnAttack(npc, target)
end

--杀死别人
function OnKill(npc, target)
end

--被别人杀死
function OnKilled(npc, attacker)
end

--被伤害
function OnDamaged(npc, dmg, attacker)
end

--死亡
function OnDied(npc)
end

--回血
function Recover(npc)
end

--在战斗中移动
function OnMovedInFightState(npc)
end

--到达
function OnArrived(npc)
end
